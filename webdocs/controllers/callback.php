<?php
function callbackhome($blockname, $blockobj) {
  switch ($blockname) {
    /*case 'clipListingBlock':
      $c = view::$blockrepeat[$blockname];
      $blockobj->frameType = ($c == 1) ? 'recentHome' : (($c == 2) ? 'popularHome' : 'foryouHome');
      break;*/
    default:
      break;
  }
}